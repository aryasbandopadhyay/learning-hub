{
  "shape": "function",
  "className": "Solution",
  "entry": "simplifyPath",
  "params": [
    "path"
  ],
  "compareMode": "exact",
  "tests": [
    {
      "id": "example-1",
      "kind": "example",
      "args": [
        "/home//foo/"
      ],
      "expected": "/home/foo"
    },
    {
      "id": "edge-1",
      "kind": "edge",
      "label": "trailing slash",
      "args": [
        "/home/"
      ],
      "expected": "/home"
    },
    {
      "id": "edge-2",
      "kind": "edge",
      "label": "double dot",
      "args": [
        "/../"
      ],
      "expected": "/"
    },
    {
      "id": "edge-3",
      "kind": "edge",
      "label": "multiple slashes",
      "args": [
        "/home//foo/"
      ],
      "expected": "/home/foo"
    },
    {
      "id": "edge-4",
      "kind": "edge",
      "label": "dot and dotdot",
      "args": [
        "/a/./b/../../c/"
      ],
      "expected": "/c"
    },
    {
      "id": "edge-5",
      "kind": "edge",
      "label": "root only",
      "args": [
        "/"
      ],
      "expected": "/"
    },
    {
      "id": "edge-6",
      "kind": "edge",
      "label": "complex",
      "args": [
        "/a/../../b/../c//.//"
      ],
      "expected": "/c"
    },
    {
      "id": "edge-7",
      "kind": "edge",
      "label": "no navigation",
      "args": [
        "/usr/local/bin"
      ],
      "expected": "/usr/local/bin"
    }
  ],
  "complexity": {
    "supported": true,
    "sizes": [
      500,
      1000,
      2000,
      4000,
      8000
    ],
    "repeats": 3,
    "genSpec": {
      "params": [
        {
          "name": "path",
          "type": "str",
          "role": "size",
          "lo": -10000,
          "hi": 10000
        }
      ]
    }
  },
  "id": "stack/12-simplify-path",
  "title": "Simplify Path",
  "difficulty": "Medium",
  "topic": "stack",
  "solutions": {
    "naive": "class Solution:\n    def simplifyPath(self, path: str) -> str:\n        parts = [p for p in path.split(\"/\") if p != \"\" and p != \".\"]\n        changed = True\n        while changed:\n            changed = False\n            for i, part in enumerate(parts):\n                if part == \"..\":\n                    start = max(0, i - 1)\n                    parts[start:i + 1] = []\n                    changed = True\n                    break\n        return \"/\" + \"/\".join(p for p in parts if p != \"..\")",
    "better": "class Solution:\n    def simplifyPath(self, path: str) -> str:\n        stack = []\n        for part in path.split(\"/\"):\n            if part == \"\" or part == \".\":\n                continue\n            if part == \"..\":\n                if stack:\n                    stack.pop()\n            else:\n                stack.append(part)\n        return \"/\" + \"/\".join(stack)",
    "optimal": "class Solution:\n    def simplifyPath(self, path: str) -> str:\n        stack = []\n        skip = {\"\", \".\"}\n        for part in path.split(\"/\"):\n            if part in skip:\n                continue\n            if part == \"..\":\n                if stack:\n                    stack.pop()\n            else:\n                # Names like \"...\" are valid directory names.\n                stack.append(part)\n        return \"/\" + \"/\".join(stack)"
  },
  "starterCode": "class Solution:\n    def simplifyPath(self, path):\n        # TODO: write your solution here\n        pass\n"
}
{
  "shape": "function",
  "className": "Solution",
  "entry": "isHappy",
  "params": [
    "n"
  ],
  "compareMode": "exact",
  "tests": [
    {
      "id": "example-1",
      "kind": "example",
      "args": [
        19
      ],
      "expected": true
    },
    {
      "id": "edge-1",
      "kind": "edge",
      "label": "happy 19",
      "args": [
        19
      ],
      "expected": true
    },
    {
      "id": "edge-2",
      "kind": "edge",
      "label": "one",
      "args": [
        1
      ],
      "expected": true
    },
    {
      "id": "edge-3",
      "kind": "edge",
      "label": "not happy 2",
      "args": [
        2
      ],
      "expected": false
    },
    {
      "id": "edge-4",
      "kind": "edge",
      "label": "happy 7",
      "args": [
        7
      ],
      "expected": true
    },
    {
      "id": "edge-5",
      "kind": "edge",
      "label": "not happy 4",
      "args": [
        4
      ],
      "expected": false
    }
  ],
  "id": "math-geometry/04-happy-number",
  "title": "Happy Number",
  "difficulty": "Easy",
  "topic": "math-geometry",
  "solutions": {
    "naive": "class Solution:\n    def isHappy(self, n: int) -> bool:\n        def nxt(x: int) -> int:\n            total = 0\n            while x:\n                digit = x % 10\n                total += digit * digit\n                x //= 10\n            return total\n\n        for _ in range(1000):\n            if n == 1:\n                return True\n            n = nxt(n)\n        return False",
    "better": "class Solution:\n    def isHappy(self, n: int) -> bool:\n        def nxt(x: int) -> int:\n            return sum(int(ch) ** 2 for ch in str(x))\n\n        seen = set()\n        while n != 1 and n not in seen:\n            seen.add(n)\n            n = nxt(n)\n        return n == 1",
    "optimal": "class Solution:\n    def isHappy(self, n: int) -> bool:\n        def nxt(x: int) -> int:\n            total = 0\n            while x:\n                digit = x % 10\n                total += digit * digit\n                x //= 10\n            return total\n\n        slow, fast = n, nxt(n)\n        while fast != 1 and slow != fast:\n            slow = nxt(slow)\n            fast = nxt(nxt(fast))\n        return fast == 1"
  },
  "starterCode": "class Solution:\n    def isHappy(self, n):\n        # TODO: write your solution here\n        pass\n"
}
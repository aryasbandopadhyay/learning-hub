{
  "shape": "function",
  "className": "Solution",
  "entry": "characterReplacement",
  "params": [
    "s",
    "k"
  ],
  "compareMode": "exact",
  "tests": [
    {
      "id": "example-1",
      "kind": "example",
      "args": [
        "AABABBA",
        1
      ],
      "expected": 4
    },
    {
      "id": "edge-1",
      "kind": "edge",
      "label": "classic ABAB k=2",
      "args": [
        "ABAB",
        2
      ],
      "expected": 4
    },
    {
      "id": "edge-2",
      "kind": "edge",
      "label": "classic AABABBA k=1",
      "args": [
        "AABABBA",
        1
      ],
      "expected": 4
    },
    {
      "id": "edge-3",
      "kind": "edge",
      "label": "single character",
      "args": [
        "A",
        0
      ],
      "expected": 1
    },
    {
      "id": "edge-4",
      "kind": "edge",
      "label": "all same, no replacement",
      "args": [
        "AAAA",
        0
      ],
      "expected": 4
    },
    {
      "id": "edge-5",
      "kind": "edge",
      "label": "k covers whole string",
      "args": [
        "ABCD",
        3
      ],
      "expected": 4
    },
    {
      "id": "edge-6",
      "kind": "edge",
      "label": "k zero mixed",
      "args": [
        "ABAB",
        0
      ],
      "expected": 1
    },
    {
      "id": "edge-7",
      "kind": "edge",
      "label": "replace to extend run",
      "args": [
        "AABBBCCC",
        2
      ],
      "expected": 5
    }
  ],
  "complexity": {
    "supported": true,
    "sizes": [
      500,
      1000,
      2000,
      4000,
      8000
    ],
    "repeats": 3,
    "genSpec": {
      "params": [
        {
          "name": "s",
          "type": "str",
          "role": "size",
          "lo": -10000,
          "hi": 10000
        },
        {
          "name": "k",
          "type": "int",
          "role": "target",
          "lo": 1,
          "hi": 10000
        }
      ]
    }
  },
  "id": "sliding-window/02-longest-repeating-character-replacement",
  "title": "Longest Repeating Character Replacement",
  "difficulty": "Medium",
  "topic": "sliding-window",
  "solutions": {
    "naive": "class Solution:\n    def characterReplacement(self, s: str, k: int) -> int:\n        best = 0\n        for left in range(len(s)):\n            counts = {}\n            max_freq = 0\n            for right in range(left, len(s)):\n                counts[s[right]] = counts.get(s[right], 0) + 1\n                max_freq = max(max_freq, counts[s[right]])\n                if right - left + 1 - max_freq <= k:\n                    best = max(best, right - left + 1)\n        return best",
    "better": "class Solution:\n    def characterReplacement(self, s: str, k: int) -> int:\n        counts = {}\n        left = best = 0\n        for right, ch in enumerate(s):\n            counts[ch] = counts.get(ch, 0) + 1\n            # Recomputing over at most 26 letters keeps this linear for uppercase input.\n            while right - left + 1 - max(counts.values()) > k:\n                counts[s[left]] -= 1\n                left += 1\n            best = max(best, right - left + 1)\n        return best",
    "optimal": "class Solution:\n    def characterReplacement(self, s: str, k: int) -> int:\n        counts = {}\n        left = best = max_freq = 0\n        for right, ch in enumerate(s):\n            counts[ch] = counts.get(ch, 0) + 1\n            max_freq = max(max_freq, counts[ch])\n            if right - left + 1 - max_freq > k:\n                counts[s[left]] -= 1\n                left += 1\n            best = max(best, right - left + 1)\n        return best"
  },
  "starterCode": "class Solution:\n    def characterReplacement(self, s, k):\n        # TODO: write your solution here\n        pass\n"
}
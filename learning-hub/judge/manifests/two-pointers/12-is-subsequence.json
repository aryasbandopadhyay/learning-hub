{
  "shape": "function",
  "className": "Solution",
  "entry": "isSubsequence",
  "params": [
    "s",
    "t"
  ],
  "compareMode": "exact",
  "tests": [
    {
      "id": "example-1",
      "kind": "example",
      "args": [
        "abc",
        "ahbgdc"
      ],
      "expected": true
    },
    {
      "id": "edge-1",
      "kind": "edge",
      "label": "classic true",
      "args": [
        "abc",
        "ahbgdc"
      ],
      "expected": true
    },
    {
      "id": "edge-2",
      "kind": "edge",
      "label": "classic false",
      "args": [
        "axc",
        "ahbgdc"
      ],
      "expected": false
    },
    {
      "id": "edge-3",
      "kind": "edge",
      "label": "empty s is always a subsequence",
      "args": [
        "",
        "anything"
      ],
      "expected": true
    },
    {
      "id": "edge-4",
      "kind": "edge",
      "label": "empty t with non-empty s",
      "args": [
        "a",
        ""
      ],
      "expected": false
    },
    {
      "id": "edge-5",
      "kind": "edge",
      "label": "both empty",
      "args": [
        "",
        ""
      ],
      "expected": true
    },
    {
      "id": "edge-6",
      "kind": "edge",
      "label": "s equals t",
      "args": [
        "abc",
        "abc"
      ],
      "expected": true
    },
    {
      "id": "edge-7",
      "kind": "edge",
      "label": "s longer than t",
      "args": [
        "abcd",
        "abc"
      ],
      "expected": false
    },
    {
      "id": "edge-8",
      "kind": "edge",
      "label": "requires order",
      "args": [
        "ba",
        "ab"
      ],
      "expected": false
    }
  ],
  "complexity": {
    "supported": true,
    "sizes": [
      500,
      1000,
      2000,
      4000,
      8000
    ],
    "repeats": 3,
    "genSpec": {
      "params": [
        {
          "name": "s",
          "type": "str",
          "role": "size",
          "lo": -10000,
          "hi": 10000
        },
        {
          "name": "t",
          "type": "str",
          "role": "const",
          "value": "ahbgdc"
        }
      ]
    }
  },
  "id": "two-pointers/12-is-subsequence",
  "title": "Is Subsequence",
  "difficulty": "Easy",
  "topic": "two-pointers",
  "solutions": {
    "naive": "class Solution:\n    def isSubsequence(self, s: str, t: str) -> bool:\n        def dfs(i: int, j: int) -> bool:\n            if i == len(s):\n                return True\n            if j == len(t):\n                return False\n            if s[i] == t[j] and dfs(i + 1, j + 1):\n                return True\n            return dfs(i, j + 1)\n\n        return dfs(0, 0)",
    "better": "class Solution:\n    def isSubsequence(self, s: str, t: str) -> bool:\n        start = 0\n        for ch in s:\n            pos = t.find(ch, start)\n            if pos == -1:\n                return False\n            start = pos + 1\n        return True",
    "optimal": "class Solution:\n    def isSubsequence(self, s: str, t: str) -> bool:\n        i = 0\n        for ch in t:\n            if i < len(s) and s[i] == ch:\n                i += 1\n        return i == len(s)"
  },
  "starterCode": "class Solution:\n    def isSubsequence(self, s, t):\n        # TODO: write your solution here\n        pass\n"
}